import 'package:flutter/material.dart';

class UnKnownRoute extends StatelessWidget {
  const UnKnownRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("don't find this widget"),);
  }
}
